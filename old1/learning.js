// Normal Function

function add(a, b) {
  return a + b;
}

// Arrow Function
const add = (a, b) => a + b;

// or

const add = (a, b) => {
  return a + b;
};

// Symbols
// for === they wont be equal ,
// they are used to make objects that must not collide;

//Adavnced Array
